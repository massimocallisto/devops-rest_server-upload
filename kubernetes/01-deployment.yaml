apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload
  namespace: gestionale
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload
  template:
    metadata:
      labels:
        app: upload
    spec:
      restartPolicy: Always
      containers:
        - name: upload
          image: massimocallisto/devops-upload-app:1.0.0-amd64
          ports:
            - containerPort: 8000
          env:
            - name: API_BEARER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: upload-secret
                  key: API_BEARER_TOKEN
          startupProbe:
            httpGet:
              path: /
              port: 8000
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 3
          volumeMounts:
            - name: uploads
              mountPath: /uploads
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: my-uploads-pvc
